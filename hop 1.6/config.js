// Bible90 deploy config (public values — Supabase ANON key is meant for client apps)
window.BIBLE90_CONFIG = {
  // Fill these before deploying (recommended) OR set them inside Admin → Backend Setup
  supabaseUrl: "",
  supabaseAnonKey: "",
  // Only this email can access Admin
  adminEmail: ""
};
